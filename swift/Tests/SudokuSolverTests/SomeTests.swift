import XCTest
@testable import SudokuSolver

class SomeTests: XCTestCase {
    func testSomething() {
        XCTAssert(true)
    }

    static let allTests = [
        ("testSomething", testSomething)
    ]
}
